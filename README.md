# golden-forest.nvim

`golden-forest.nvim` is the Neovim colorscheme for the broader **Golden-Forest** theme ecosystem.

This repo contains the Neovim theme itself. It is intended to pair with the Omarchy theme repo:

- **Omarchy theme repo:** `omarchy-golden-forest-theme`
- **Neovim theme repo:** `golden-forest.nvim`

## What this repo is

This repo is focused on Neovim only.

It provides the editor colorscheme, highlight groups, and plugin styling needed for Neovim to match the rest of the Golden-Forest theme.

## Relationship to Omarchy

`golden-forest.nvim` is the editor-side counterpart to `omarchy-golden-forest-theme`.

That means:

- `golden-forest.nvim` handles Neovim colors and highlights
- `omarchy-golden-forest-theme` handles desktop and app theming for Omarchy
- both are meant to share the same palette and visual direction

## Theme pairing

Use these together for a matching setup:

- **Neovim:** `golden-forest.nvim`
- **Omarchy:** `omarchy-golden-forest-theme`

## Goal

The goal of `golden-forest.nvim` is to make Neovim feel like a natural part of the wider Golden-Forest environment instead of a separate, mismatched editor theme.
